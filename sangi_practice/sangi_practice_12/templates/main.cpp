#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int n, w;
    cin >> n >> w;
    for (int i = 0; i < n; i++) {
        int cost, value;
        cin >> cost >> value;
        // ここで DP を更新する
    }

    // ここで予算内の最大価値を出力する

    return 0;
}
