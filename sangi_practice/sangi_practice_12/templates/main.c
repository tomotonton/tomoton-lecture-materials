#include <stdio.h>

int main(void) {
    int n, w, i, cost, value;
    scanf("%d %d", &n, &w);
    for (i = 0; i < n; i++) {
        scanf("%d %d", &cost, &value);
        /* ここで DP を更新する */
    }

    /* ここで予算内の最大価値を出力する */

    return 0;
}
