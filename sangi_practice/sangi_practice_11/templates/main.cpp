#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        long long s, e;
        cin >> s >> e;
        // ここでイベント（開始 +1 / 終了 -1）として記録する
    }

    // ここでイベントを並べ替えて、最大重なり数を求めて出力する

    return 0;
}
