#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        long long a;
        cin >> a;
        // ここで GCD と LCM を更新していく
    }

    // ここで GCD と LCM を出力する

    return 0;
}
