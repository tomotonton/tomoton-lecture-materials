#include <stdio.h>

int main(void) {
    int n, i;
    long long a;
    scanf("%d", &n);
    for (i = 0; i < n; i++) {
        scanf("%lld", &a);
        /* ここで GCD と LCM を更新していく */
    }

    /* ここで GCD と LCM を出力する */

    return 0;
}
