# 05：論理演算とswitch文（Cとの違い）

---

## 🎯 このコマのねらい

- 論理演算子（AND, OR, NOT）を使って複数条件を記述する
- `switch` 文による分岐処理を学ぶ
- Cとの違いを通じてC++の分かりやすさと安全性を体感する

---

## 📘 C++とCの違い（論理演算・switch）

- `bool` 型があるため、`true` や `false` を使って分かりやすい条件式が書ける（Cでは0か1）
- C++ の `switch` 文では `enum class` を安全に使える（Cでは `int` 相当の値のみ）
- `string` 型は `switch` 文では使えないため、文字列には `if` 文を使う

### 💡補足：列挙体（enum）とは？

`enum` は **列挙体（列挙型）**と呼ばれ、関連する定数を1つの名前空間にまとめて扱える仕組みです。
たとえば以下のように定義します：

```cpp
enum Color { Red, Green, Blue };
```

これは `Red=0`, `Green=1`, `Blue=2` というように、自動的に整数値が割り振られます。

- `enum` は整数として扱えるため、`switch` 文との相性が良い
- C++ の `enum class` は、スコープ付きで他の定数と衝突しない安全な列挙が可能

---

## 💻 実践ステップ

### 05_01_logical.cpp（AND条件）
```cpp
#include <iostream>
using namespace std;

int main() {
    int age;
    bool hasTicket;
    cout << "年齢を入力：";
    cin >> age;
    cout << "チケットを持っていますか？（1: はい / 0: いいえ）：";
    cin >> hasTicket;

    if (age >= 18 && hasTicket) {
        cout << "入場できます。" << endl;
    } else {
        cout << "入場できません。" << endl;
    }
    return 0;
}
```

---

### 05_02_logical_or.cpp（OR条件）
```cpp
#include <iostream>
using namespace std;

int main() {
    bool hasMemberCard;
    bool hasCoupon;
    cout << "会員カードを持っていますか？（1: はい / 0: いいえ）：";
    cin >> hasMemberCard;
    cout << "クーポンを持っていますか？（1: はい / 0: いいえ）：";
    cin >> hasCoupon;

    if (hasMemberCard || hasCoupon) {
        cout << "割引適用！" << endl;
    } else {
        cout << "通常価格です。" << endl;
    }
    return 0;
}
```

---

### 05_03_logical_not.cpp（NOT条件）
```cpp
#include <iostream>
using namespace std;

int main() {
    bool isBanned;
    cout << "利用制限されていますか？（1: はい / 0: いいえ）：";
    cin >> isBanned;

    if (!isBanned) {
        cout << "利用可能です。" << endl;
    } else {
        cout << "利用制限中です。" << endl;
    }
    return 0;
}
```

---

### 05_04_switch_basic.cpp（switch文の基本）
```cpp
#include <iostream>
using namespace std;

int main() {
    int rank;
    cout << "ランク（1〜3）を入力してください：";
    cin >> rank;

    switch (rank) {
        case 1:
            cout << "金賞！" << endl;
            break;
        case 2:
            cout << "銀賞！" << endl;
            break;
        case 3:
            cout << "銅賞！" << endl;
            break;
        default:
            cout << "参加賞！" << endl;
            break;
    }
    return 0;
}
```

---

### 05_05_switch_enum_class.cpp（enum classとの組み合わせ）
```cpp
#include <iostream>
using namespace std;

enum class Rank { Gold = 1, Silver, Bronze };

int main() {
    int input;
    cout << "ランク（1:Gold, 2:Silver, 3:Bronze）を入力してください：";
    cin >> input;
    Rank r = static_cast<Rank>(input);

    switch (r) {
        case Rank::Gold:
            cout << "金賞！" << endl;
            break;
        case Rank::Silver:
            cout << "銀賞！" << endl;
            break;
        case Rank::Bronze:
            cout << "銅賞！" << endl;
            break;
    }
    return 0;
}
```
💡 `enum class` を使うと、定数名の衝突を避けつつ、安全に分岐処理ができます。

---

## 🔍 演習

以下のプログラムはすべて、キーボードから値を入力させる形式で作成してください。

1. 年齢とチケット所持を入力させ、「18歳以上かつチケットあり」なら「OK」、それ以外は「NG」と表示してください（05_06_entry_check.cpp）
2. `switch` を使って曜日番号（1〜7）を入力し、対応する曜日名を表示してください（05_07_weekday.cpp）
3. ログイン状態を 1（ログイン済み）または 0（未ログイン）として入力させ、`!` を使って未ログイン時に「ログインしてください」と表示してください（05_08_login_not.cpp）

---

## ✅ まとめ

- 論理演算（AND, OR, NOT）で複数条件を扱えるようになった
- `switch` 文を使って分岐処理を整理できるようになった
- `enum class` の導入で、型安全な分岐も学んだ

