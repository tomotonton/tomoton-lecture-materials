# 06：繰り返し処理（for文・while文）

---

## 🎯 このコマのねらい

- `for` 文と `while` 文による繰り返し処理を学ぶ
- `break` / `continue` を使ったループ制御を理解する
- `string` や `vector` を使った繰り返し処理の実践例に触れる

---

## 📘 C++とCの違い（繰り返し処理）

- 基本構文（for, while, do-while）はCと同じ
- `bool` 型で条件を管理できるため、読みやすい条件式が書ける
- C++標準ライブラリの `string` や `vector` と組み合わせて使える

### 🔍 設計思想の違い：変数の宣言位置

C言語では「関数の先頭で変数をすべて宣言する」のが基本でした。
→ これは「処理の途中で変数を定義すると管理しづらい」という考え方に基づいています。

C++では、**必要な場所で必要な変数を定義する**ことが許されています。
→ 意図が明確になり、コードの見通しもよくなります。

```cpp
for (int i = 0; i < 5; i++) {  // C++ではここで変数宣言が可能
    cout << i << endl;
}
```

この柔軟さが、C++の「より直感的に」「より安全に」書ける設計思想を表しています。

- 基本構文（for, while, do-while）はCと同じ
- `bool` 型で条件を管理できるため、読みやすい条件式が書ける
- C++標準ライブラリの `string` や `vector` と組み合わせて使える

---

## 💻 実践ステップ

### 06_01_for_basic.cpp（for文の基本）
```cpp
#include <iostream>
using namespace std;

int main() {
    for (int i = 0; i < 5; i++) {
        cout << i << "番目のループ" << endl;
    }
    return 0;
}
```

---

### 06_02_while_basic.cpp（while文の基本）
```cpp
#include <iostream>
using namespace std;

int main() {
    int count = 0;
    while (count < 5) {
        cout << count << "回目です" << endl;
        count++;
    }
    return 0;
}
```

---

### 06_03_input_until_ok.cpp（ユーザーの入力による終了）
```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string input;
    while (true) {
        cout << "OKと入力してください：";
        cin >> input;
        if (input == "OK") break;
    }
    cout << "終了します。" << endl;
    return 0;
}
```

---

### 06_04_continue_break.cpp（continue / break）
```cpp
#include <iostream>
using namespace std;

int main() {
    for (int i = 1; i <= 10; i++) {
        if (i % 3 == 0) continue; // 3の倍数はスキップ
        if (i > 8) break;         // 8を超えたら終了
        cout << i << endl;
    }
    return 0;
}
```

---

### 06_05_string_loop.cpp（stringの文字を順に表示）
```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string word;
    cout << "文字列を入力してください：";
    cin >> word;

    for (int i = 0; i < word.size(); i++) {
        cout << i << ": " << word[i] << endl;
    }
    return 0;
}
```

---

### 06_06_vector_loop.cpp（vectorで繰り返し）
```cpp
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> scores;
    int num;
    cout << "点数を5つ入力してください：" << endl;
    for (int i = 0; i < 5; i++) {
        cin >> num;
        scores.push_back(num);
    }

    for (int i = 0; i < scores.size(); i++) {
        cout << i + 1 << "人目: " << scores[i] << "点" << endl;
    }
    return 0;
}
```

---

## 🔍 演習（すべてキーボード入力を使用）

1. 任意の数 `n` を入力し、1〜`n` までの合計を出力してください（06_07_sum_to_n.cpp）
2. 数値を繰り返し入力させ、0が入力されたら終了、合計を出力してください（06_08_sum_until_zero.cpp）
3. 任意の文字列を繰り返し入力させ、「end」と入力されたら終了してください（06_09_input_until_end.cpp）

---

## 📘 vectorとは？

`vector` は C++ の標準ライブラリにある **「可変長の配列」** です。

- 要素を追加するたびにサイズが自動で増える
- `[]` でアクセスできる
- `.size()` で要素数を取得できる
- `push_back()` で末尾に要素を追加できる

```cpp
vector<int> data;
data.push_back(100);
data.push_back(200);
cout << data[0]; // 100
```

---

## ✅ まとめ

- `for` と `while` の使い分けを理解した
- `break` や `continue` によるループ制御ができるようになった
- `string` や `vector` と組み合わせた実用的な繰り返しが書けるようになった

